//write a function sumNums
//get the number
//convert the number into a string, the string into an array so that we can access each digit of the number
//check if the array's length is greater than one
//if greateradd the two or more digits in the array
//save the sum as the new number
//repeat steps 3  & 4
// if not greater; return the number
// 

//parseInt(numstring) or put a + before a variable

function sumNums(num) {
    let number = num;
    let stringNum =number.toString()
    if()
    
}